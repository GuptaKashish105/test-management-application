# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty: no source files, no commits, and no package/build configuration exist yet (`git log` shows no commits on `master`). There is nothing to build, lint, or test.

When code is added to this project, update this file with:

1. Build, lint, and test commands (including how to run a single test).
2. High-level architecture notes once a real structure exists.
